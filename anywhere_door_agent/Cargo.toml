[package]
name = "anywhere_door_agent"
version = "0.1.0"
edition = "2024"

[dependencies]
ctrlc = "3.4"
walkdir = "2"
notify = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(windows)'.dependencies]
windows-service = "0.8"
